# CI/CD Pipeline - TEMPORÄR DEAKTIVIERT wegen Email-Spam
# Um wieder zu aktivieren, einfach die Kommentare bei "on:" entfernen

# name: CI/CD Pipeline

# on:
#     push:
#         branches: ['main']
#     pull_request:
#         branches: ['main']

# jobs:
#     test:
#         name: Lint & Test
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout Code
#               uses: actions/checkout@v4

#             - name: Setup Node.js
#               uses: actions/setup-node@v4
#               with:
#                   node-version: '20.x'
#                   cache: 'npm'

#             - name: Install Dependencies
#               run: npm ci

#             - name: Run Linter (ESLint & Prettier)
#               run: npm run lint

#             - name: Run Type Checking (JSDoc + TS)
#               run: npm run typecheck

#             - name: Run API Tests (Jest)
#               run: npm test

#             - name: Install Playwright Browsers
#               run: npx playwright install --with-deps

#             - name: Run Frontend E2E Tests (Playwright)
#               run: npm run test:e2e

name: CI/CD Pipeline (Disabled)

on:
    workflow_dispatch:  # Nur manuell startbar

jobs:
    test:
        name: Tests Disabled
        runs-on: ubuntu-latest
        steps:
            - name: Skip
              run: echo "CI/CD temporär deaktiviert - keine Emails mehr!"
